@using Microsoft.AspNetCore.Mvc.Localization
@inject IViewLocalizer Localizer
@model CrowdFundingApp.ViewModels.CompanyProfileViewModel
@{
    ViewData["Title"] = "Add Bonus";
}

<div class="row justify-content-center">
    <div class="col-4">
        <form asp-action="saveBonus" asp-controller="CompanyProfile" asp-route-companyId="@Model.companyProfile.companyId">
            <div class="form-group">
                <label asp-for="@Model.bonus.bonusName">@Localizer["BonusName"]</label>
                <input required class="form-control" type="text" asp-for="@Model.bonus.bonusName" />
            </div>
            <div class="form-group">
                <label asp-for="@Model.bonus.bonusCost">@Localizer["BonusCost"]</label>
                <input required class="form-control" asp-for="@Model.bonus.bonusCost" />
            </div>
            <div class="form-group">
                <label asp-for="@Model.bonus.aboutBonus">@Localizer["About"]</label>
                <textarea required class="form-control" asp-for="@Model.bonus.aboutBonus"></textarea>
            </div>
            <div class="form-group">
                <input class="btn btn-primary" type="submit" value="@Localizer["Add"]" />
            </div>
        </form>
    </div>
</div>

